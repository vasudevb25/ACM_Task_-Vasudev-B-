import sys
import math


t = int(input().strip())
for a0 in range(t):
    n = int(input().strip())
    while n % 2 == 0:
        n //= 2
    for i in range(3, int(math.sqrt(n)) + 1, 2):
        while n % i == 0:
            n //= i
    print(n)
